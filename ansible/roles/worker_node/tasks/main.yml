---

- name: Add worker node
  become: yes
  shell: |
    kubeadm join {{ hostvars[groups['master'][0]].ansible_host }}:6443 --token={{ hostvars[groups['master'][0]]['kubeadm_token'].stdout }} --discovery-token-ca-cert-hash sha256:{{ hostvars[groups['master'][0]]['kubeadm_ca_cert'].stdout }} --v=5